import React from 'react';

// This component is deprecated.
// The "Now Playing" functionality has been integrated directly into components/AudioPlayer.tsx
// to provide a more robust and self-contained solution.
// This file is kept for reference but is no longer used in the application.

const NowPlaying: React.FC = () => {
  return null; // Render nothing
};

export default NowPlaying;
